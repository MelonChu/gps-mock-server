# ============================================================
# Pikmin Bloom GPS 模擬器 — Python 依賴套件
# 桌面客戶端 (gps_mock_client.py)
# ============================================================
# 注意：核心功能使用 Python 標準函式庫即可運行。
# 下方列出的套件僅為選擇性功能。

# --- iOS 裝置偵測 (選擇性) ---
# 啟用方式: gps_mock_client.py --ios
# 僅支援 macOS / Linux
# pymobiledevice3>=0.0.1
