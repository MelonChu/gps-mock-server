<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gpsmock.server">

    <!-- ====================================================================
         權限宣告
         ====================================================================

         INTERNET:              TCP Socket 伺服器連線
         ACCESS_FINE_LOCATION:  用於檢查 LocationManager 狀態
         ACCESS_MOCK_LOCATION:  模擬 GPS 位置 (注意：此權限在 Android 6.0+
                                無法在運行時請求，需透過開發者選項設定)
         FOREGROUND_SERVICE:    前景服務 (Android 9+ 必要)
         WAKE_LOCK:             防止手機在背景休眠
         ACCESS_NETWORK_STATE:  檢查 Wi-Fi 連線
         ACCESS_WIFI_STATE:     取得 Wi-Fi IP

        注意：ACCESS_MOCK_LOCATION 僅在 Android 的開發者選項中
        設定此 App 為「模擬位置資訊應用程式」後才會生效。
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.healthconnect.READ_STEPS" />
    <uses-permission android:name="android.permission.healthconnect.WRITE_STEPS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light"
        android:usesCleartextTraffic="true"
        tools:targetApi="33">

        <!-- ===============================================================
             Main Activity — 使用者介面
             用於啟動／停止伺服器，顯示 IP 與狀態
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ===============================================================
             GPSServerService — 前景服務
             負責 TCP 監聽與 GPS 座標注入
        -->
        <service
            android:name=".GPSServerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".FloatingControlService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".GpsMockService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
